#include <string>
#include <vector>
using namespace std;

bool findChar(char x, string y);
void formatting(vector<string> t);
/*
******Note : you should provide vector t to function as below ******
vector<string> t;
string text;
string x = "";
ifstream fileRead("x.xml");
while (getline(fileRead, text)) {
	t.push_back(text);
	x += t.back();
}
fileRead.close();
formatting(t);
*/